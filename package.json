{
  "name": "electron-app",
  "version": "1.0.0",
  "description": "an electron app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --exec electron ."
  },
  "author": "pixelator",
  "license": "MIT",
  "devDependencies": {
    "electron": "^25.3.2"
  }
}
